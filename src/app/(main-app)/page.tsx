import { Metadata } from "next";
import App from "./app";

export const metadata: Metadata = {
  title: "Display - Magic Display",
  description: "The Display page of Magic Display.",
};

export default App;
